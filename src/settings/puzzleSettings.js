// Puzzle configuration file
const puzzleConfig = {
  // Default puzzle ID to show when no specific ID is provided in URL
  // Set to null to show the daily puzzle based on date
  // Set to a specific number (e.g., 15) to always show that puzzle by default
  defaultPuzzleId: 1,  // Change this value to set a different default puzzle
  
  // Other configuration options can be added here in the future
  // For example: difficulty levels, puzzle categories, etc.
};

export default puzzleConfig;